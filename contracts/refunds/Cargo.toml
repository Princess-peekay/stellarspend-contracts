[package]
name = "refunds"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "../refunds.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
